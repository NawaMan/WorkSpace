###############################################################################
# Workspace Docker Arguments File (workspace-docker.args)
#
# Each non-empty, non-comment line is passed directly to `docker run`.
# This lets you extend or override the container’s runtime configuration.
#
# Typical use cases:
#   • Expose additional ports
#   • Bind mount host directories (e.g., configs, caches, SSH keys)
#   • Inject environment variables
#   • Adjust resources (memory, CPUs, shm-size)
#   • Add custom host mappings or networks
#
# Notes:
#   - Quotes are respected (spaces in paths are fine).
#   - Comments (# ...) and blank lines are ignored.
#   - CLI args (workspace.sh -- ...) always override these settings.
###############################################################################

### -------------------------------------------------------------------------
### Port mappings
### -------------------------------------------------------------------------

# Local-only port forward (only accessible from host):
# -p 127.0.0.1:9000:9000

# Public port forward (accessible from LAN/Internet):
# -p 8080:8080


### -------------------------------------------------------------------------
### Volume mounts (host → container)
### -------------------------------------------------------------------------

# Maven cache (to avoid redownloading dependencies):
# -v "$HOME/.m2:/home/coder/.m2"

# Node/npm cache:
# -v "$HOME/.npm:/home/coder/.npm"

# Python pip cache:
# -v "$HOME/.cache/pip:/home/coder/.cache/pip"

# SSH keys for Git operations:
# -v "$HOME/.ssh:/home/coder/.ssh:ro"

# AWS credentials:
# -v "$HOME/.aws:/home/coder/.aws:ro"

# Shell config (bash/zsh):
# -v "$HOME/.bashrc:/home/coder/.bashrc:ro"
# -v "$HOME/.zshrc:/home/coder/.zshrc:ro"

# Dotfiles repo (if you manage configs via git):
# -v "$HOME/.dotfiles:/home/coder/.dotfiles:ro"


### -------------------------------------------------------------------------
### Environment variables
### -------------------------------------------------------------------------

# Inject AWS region/profile directly:
# -e AWS_DEFAULT_REGION=us-east-1
# -e AWS_PROFILE=dev

# Custom application tokens:
# -e GH_TOKEN=ghp_xxxxxxxxx
# -e OPENAI_API_KEY=sk-xxxxxxx

# Timezone:
# -e TZ=Asia/Bangkok


### -------------------------------------------------------------------------
### Resource tuning
### -------------------------------------------------------------------------

# Shared memory size (useful for browsers, ML, etc.):
# --shm-size=2g

# Limit memory:
# --memory=4g

# Limit CPU usage:
# --cpus=2


### -------------------------------------------------------------------------
### Networking
### -------------------------------------------------------------------------

# Add a custom hostname mapping:
# --add-host "minio.local:127.0.0.1"

# Join a specific Docker network:
# --network=my-docker-net


### -------------------------------------------------------------------------
### Miscellaneous
### -------------------------------------------------------------------------

# Auto-restart container on reboot or crash:
# --restart unless-stopped

# Privileged mode (not recommended unless absolutely needed):
# --privileged
