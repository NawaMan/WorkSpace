# ws--config.toml - Workspace Configuration Example
# This file demonstrates TOML-based configuration for the workspace tool

# Workspace settings
variant = "codeserver"
version = "latest"

# Runtime flags
daemon = true
verbose = false

# Additional docker run arguments
run-args = "-v;/data:/data;-e;CUSTOM_VAR=value"

# Build arguments (for local builds)
build-args = "--build-arg;NODE_VERSION=20"
